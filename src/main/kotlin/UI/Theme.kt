package UI

import java.awt.Color

open class Theme {
    val FontColor = Color.decode("#00BFFF")
    val BackgroundColor = Color.BLACK
    val  BorderColor = Color.GRAY

    val SecondaryFontColor = Color.BLACK
    val SecondaryBackgroundColor = Color.gray
    val  SecondaryBorderColor = Color.DARK_GRAY
}