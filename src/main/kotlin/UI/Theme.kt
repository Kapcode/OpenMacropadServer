package UI

import java.awt.Color

open class Theme {
    val FontColor = Color.decode("#00BFFF") // Deep Sky Blue
    val BackgroundColor = Color.BLACK // Black
    val BorderColor = Color.GRAY // Gray

    val SecondaryFontColor = Color.decode("#66D9EF") // Light Blue/Cyan
    val SecondaryBackgroundColor = Color.BLACK // Black
    val SecondaryBorderColor = Color.decode("#666666") // Dark Gray

    val PrimaryButtonColor = Color.decode("#66D9EF") // Light Blue/Cyan
    val PrimaryButtonBorder = Color.decode("#666666") // Dark Gray
    val PrimaryButtonFont = Color.BLACK // Black

    val SecondaryButtonColor = Color.decode("#F7F7F7") // Very Light Gray/Off-White
    val SecondaryButtonBorder = Color.decode("#666666") // Dark Gray
    val SecondaryButtonFont = Color.decode("#00BFFF") // Deep Sky Blue

    val ThirdButtonColor = Color.decode("#666666") // Dark Gray
    val ThirdButtonBorder = Color.decode("#666666") // Dark Gray
    val ThirdButtonFont = Color.decode("#00BFFF") // Deep Sky Blue


    val PrimaryToolBarBackgroundColor = Color.decode("#666666") // Dark Gray
    val PrimaryToolBarTooltipFontColor = Color.WHITE // WHITE
    val PrimaryToolBarBorderColor = Color.decode("#66D9EF") // Light Blue/Cyan
}